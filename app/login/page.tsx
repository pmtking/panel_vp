import useAuth from "@/hooks/useAuth";

const LoginPage = () => {
    const {login} = useAuth()
    const HanddleLogin = () => {
        
    }
    return (
        <>

        </>
    )
}

export default LoginPage ;